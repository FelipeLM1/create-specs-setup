import { PrototypeCard } from '../../core/models/prototype-card.model';

/**
 * Catálogo central de protótipos.
 * Cada card deve informar `sprint` (= N de `sprints/sprint-{N}/` da feature).
 * A home filtra por nome (busca) e por sprint (badge).
 */
export const prototypeCatalog: PrototypeCard[] = [];
